events {
  worker_connections  1024;
}

http {
  upstream app {
    server: app:80;
  }

  root /var/www/example.com;

  server {
      listen 80;

      location / {
        index index.html;
        try_files $uri $uri/ /index.html;
      }
  }
}
